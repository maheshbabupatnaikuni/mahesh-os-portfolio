import { ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Project } from '../data/projects'

export function ProjectCard({ project }: { project: Project }) {
  return <article className="project-card" style={{ '--project-accent': project.accent } as React.CSSProperties} data-cursor="OPEN"><Link to={`/projects/${project.slug}/`} className="project-image" aria-label={`View ${project.name} case study`} data-analytics-event="project_card_click" data-analytics-project-id={project.slug} data-analytics-project-name={project.name} data-analytics-location="project_image"><img src={`${import.meta.env.BASE_URL}${project.image.replace(/^\//, '')}`} alt={`${project.name} interface concept`} loading="lazy" /></Link><div className="project-content"><div className="project-meta"><span>CHAPTER {project.number} / 03</span><span>{project.category}</span></div><span className="project-status">{project.status}</span><h3>{project.name}</h3><p>{project.summary}</p><div className="project-tags">{project.technologies.slice(0, 5).map((tech) => <span key={tech}>{tech}</span>)}</div><div className="project-actions">{project.liveUrl ? <a href={project.liveUrl} target="_blank" rel="noreferrer" data-analytics-event="project_live_click" data-analytics-project-id={project.slug} data-analytics-project-name={project.name} data-analytics-location="project_card">{project.actionLabel || 'Open live site'} <ArrowUpRight size={15}/></a> : <span>{project.liveLabel || 'Still being built'}</span>}{project.githubUrl && <a href={project.githubUrl} target="_blank" rel="noreferrer" data-analytics-event="project_repository_click" data-analytics-project-id={project.slug} data-analytics-project-name={project.name} data-analytics-location="project_card">GitHub <ArrowUpRight size={15}/></a>}<Link to={`/projects/${project.slug}/`} data-analytics-event="project_card_click" data-analytics-project-id={project.slug} data-analytics-project-name={project.name} data-analytics-location="read_story">Read the story <ArrowUpRight size={15}/></Link></div></div></article>
}
