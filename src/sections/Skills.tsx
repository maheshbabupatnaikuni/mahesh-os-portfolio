import { SectionHeading } from '../components/SectionHeading'
import { skillGroups } from '../data/skills'

export function Skills() { return <section id="skills" className="section skills story-section"><SectionHeading index="04" eyebrow="THE TOOLKIT / WHAT I USE" title="A small toolkit, used with purpose." description="No percentages and no long technology list—only tools connected to work I have actually done."/><div className="skill-groups simplified">{skillGroups.map((group, index) => <article className="skill-group" key={group.category}><span className="skill-group-number">0{index + 1}</span><h3>{group.category}</h3><p className="skill-context">{group.context}</p><div className="skill-list">{group.skills.map((skill) => <div className="skill-pill" key={`${group.category}-${skill.name}`} tabIndex={0}><span>{skill.name}<small>{skill.proficiencyLabel}</small></span><p>{skill.description}</p></div>)}</div></article>)}</div></section> }
